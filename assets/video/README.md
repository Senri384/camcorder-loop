# 视频资产目录

最终 MP4 使用 `video-manifest.js` 中的文件名放在本目录。

当前成片为 Seedance 2.0 native audio 音画同出版本，包含 AAC 44.1kHz 双声道现场声。网页播放器默认启用声音。

清单中的 `status`：

- `planned`：尚未生成，视频模式自动回退文字。
- `ready`：文件已验收并可在网页播放。
- `rejected`：已有输出但未通过一致性或动作验收，不会上线。

网页状态机只读取 `visualKey`，不会依赖视频时长决定剧情，因此缺片、加载失败或网络缓慢都不会改变游戏逻辑。
